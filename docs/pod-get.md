# nv pod get
Show one instance's full record.

```
nv pod get <id> [--jq <filter>] [--json]
```

## Arguments

```
  <id>           instance id — from `nv pod list`
```

**API:** `GET /gpus/v2/instances/{id}`

