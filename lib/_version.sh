#!/usr/bin/env bash
# Stamped by make package / the release workflow; a dev checkout ships
# 0.0.0-dev and bin/nv falls back to `git describe`.
# shellcheck disable=SC2034 # consumed by bin/nv's nv::version
NV_VERSION="0.0.0-dev"
